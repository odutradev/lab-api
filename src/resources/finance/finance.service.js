import accountModel from "../../models/account";

export default class Service {
    async createAccount(){
        try {

        } catch (err) {
            return { error: "internal_error" };
        }
    };
};